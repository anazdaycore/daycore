# ── Go 构建产物 ──
/bin/
/daycore
/bundle          # go build ./api/spec/bundle drops it here
*.exe
*.test
*.out

# ── 本地 SQLite 数据库 ──
*.db
*.db-shm
*.db-wal
/data/

# ── 密钥 / 本地配置（绝不入库）──
.env
.env.local
.env.*.local
config/oauth.yaml
*.pem

# ── Node ──
node_modules/
.npm
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── 前端构建产物（不锚定层级，覆盖 web/frontend/dist 等所有 dist）──
dist/
.vite/

# ── 归档 / 大二进制 ──
*.zip
*.tgz

# ── 日志 / 覆盖率 ──
*.log
logs/
coverage/
*.lcov

# ── AI 助手的本地工作目录（.github 是 CI，必须留下）──
# 这些是每台机器各自的工具配置与草稿，不是仓库的一部分：里面记的是某个人
# 批准过哪些命令、本地路径长什么样。公开出去只会让别人读到与代码无关的
# 私人痕迹，还会在多机之间互相覆盖。
.claude/
.agents/
.codex/
.cursor/
.aider*

# ── 编辑器 / 操作系统 ──
.DS_Store
*.swp
*~
*.bak
*.orig
.idea/
.vscode/
.fuse_hidden*
.directory
.Trash-*
.nfs*

# Scratch test files left behind by review subagents. They are probes, not
# deliverables — anything worth keeping gets rewritten as a named regression
# test. Ignored so a `git add -A` during a running review cannot commit them.
internal/**/zz_*_test.go
internal/**/zzz_*_test.go
internal/**/zzprobe_test.go
web/console/node_modules/
packages/*/node_modules/


.npm-cache/
.dsh-plugins/
demo.db*

# Scratch / demo artifacts — probes, smoke scripts, screenshots, prototype
# copies. Not deliverables; a git add -A must never pick them up.
.tmp-*
Daycore前端 (1)/

# 一次性会话导出（Claude Code 与 DSH 的 daycore 会话）。刻意不带点前缀：点开头
# 的目录 VS Code 侧栏默认不显示，而这份就是给人翻的。仍然不入库。
/export-daycore/

